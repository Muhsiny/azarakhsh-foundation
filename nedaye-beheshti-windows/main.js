const {app,BrowserWindow,shell,session}=require('electron');const ORIGIN='https://app-hs2thc.v2.appdeploy.ai';function create(){const win=new BrowserWindow({width:440,height:900,minWidth:380,minHeight:650,autoHideMenuBar:true,backgroundColor:'#fbf6ed',title:'ندای بهشتی',webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true,webSecurity:true}});win.loadURL(ORIGIN+'/');win.webContents.setWindowOpenHandler(({url})=>{if(url.startsWith(ORIGIN))return {action:'allow'};shell.openExternal(url);return {action:'deny'}});win.webContents.on('will-navigate',(e,url)=>{if(!url.startsWith(ORIGIN)){e.preventDefault();shell.openExternal(url)}})}app.whenReady().then(()=>{app.setAppUserModelId('org.nedayebeheshti.desktop');session.defaultSession.setPermissionRequestHandler((_wc,permission,callback)=>callback(['notifications','geolocation','media'].includes(permission)));create();app.on('activate',()=>{if(BrowserWindow.getAllWindows().length===0)create()})});app.on('window-all-closed',()=>{if(process.platform!=='darwin')app.quit()});